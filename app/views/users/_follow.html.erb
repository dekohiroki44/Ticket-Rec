<%= form_with model: current_user.active_relationships.build, remote: true, class: "follow-form" do |f| %>
  <%= hidden_field_tag :followed_id, @user.id %>
  <%= button_tag type: :submit do %>
    <small>フォロー<br>する</small><br><i class="far fa-user mb-2"></i>
  <% end %>
<% end %>