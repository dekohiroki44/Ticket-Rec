<span class="like">
  <% if ticket.like?(current_user) %>
    <%= form_for(ticket.likes.find_by(user_id: current_user.id), method: :delete, remote: true) do |f| %>
      <%= button_tag(class: "btn btn-default btn-xs") do %>
      <i class="fas fa-thumbs-up"><%= ticket.likes.count %></i>
      <% end %>
    <% end %>
  <% else %>
    <%= form_for(ticket.likes.build, remote: true) do |f| %>
      <div><%= hidden_field_tag :ticket_id, ticket.id %></div>
      <%= button_tag(class: "btn btn-default btn-xs") do %>
      <i class="far fa-thumbs-up"><%= ticket.likes.count %></i>
      <% end %>
    <% end %>
  <% end %>
</span>

